{
  "$schema": "http://json-schema.org/schema",
  "title": "Jest runner for build facade",
  "description": "Schema for testing NG-QT apps using Jest",
  "properties": {
    "configPath": {
      "type": "string",
      "description": "Path to Jest config file",
      "default": "jest.config.js"
    }
  }
}
